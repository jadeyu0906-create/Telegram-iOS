# 发现页（Discover）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现发现页 Tab（顶部 Segmented Control + 探索生态 1:1 还原 + Web3 钱包留白），替换当前占位页。

**Architecture:** SwiftUI 实现，放 `TelegramCustom/Sources/Features/Discover/`，数据模型独立（Codable，后续换接口），用 `DiscoverViewController`（UIKit 容器 + UIHostingController）接入 Tab。

**Tech Stack:** SwiftUI, UIKit, Display, Bazel

---

## 文件结构

### 新建
```
submodules/TelegramCustom/Sources/Features/Discover/
├── Models/DiscoverModels.swift
├── ViewModels/DiscoverViewModel.swift
├── Views/
│   ├── DiscoverScreenView.swift
│   ├── DiscoverExploreView.swift
│   ├── DiscoverWalletPlaceholderView.swift
│   └── Components/
│       ├── HeroBannerCarousel.swift
│       ├── ToolGridView.swift
│       └── DAppListView.swift
└── DiscoverViewController.swift
```

### 修改
- `submodules/TelegramCustom/Sources/Core/Utils/ColorPalette.swift` — 补 textSub 颜色
- `submodules/TelegramCustom/BUILD` — 添加 Discover 模块
- `submodules/TelegramCustom/Sources/TelegramCustom.swift` — 导出 Discover
- `submodules/TelegramCustom/Resources/Localizations/{zh-Hans,en,ja,ko}.lproj/Localizable.strings` — 多语言
- `submodules/TelegramUI/Sources/TelegramRootController.swift` — 替换占位页为 DiscoverViewController

---

## Task 1: 补充 ColorPalette 颜色

**Files:**
- Modify: `submodules/TelegramCustom/Sources/Core/Utils/ColorPalette.swift`

- [ ] **Step 1: 补充 textSub（次要文字 #A1A1A1）**

在 `txtMuted` 定义后添加：

```swift
    /// 次要文字（原型 txtSub）
    public static let textSub = Color(hex: "A1A1A1")
```

- [ ] **Step 2: 编译验证**

Run: `./dev/run-simulator.sh`
Expected: 编译成功

---

## Task 2: 数据模型

**Files:**
- Create: `submodules/TelegramCustom/Sources/Features/Discover/Models/DiscoverModels.swift`

- [ ] **Step 1: 创建数据模型**

```swift
import Foundation

/// Banner 轮播项
public struct DiscoverBanner: Identifiable, Codable {
    public let id: String
    public let tag: String
    public let title: String
    public let subtitle: String
    public let actionTitle: String

    public init(id: String, tag: String, title: String, subtitle: String, actionTitle: String) {
        self.id = id
        self.tag = tag
        self.title = title
        self.subtitle = subtitle
        self.actionTitle = actionTitle
    }
}

/// 核心工具
public struct DiscoverTool: Identifiable, Codable {
    public let id: String
    public let iconSystemName: String
    public let title: String
    public let subtitle: String
    public let subtitleIsAccent: Bool

    public init(id: String, iconSystemName: String, title: String, subtitle: String, subtitleIsAccent: Bool = false) {
        self.id = id
        self.iconSystemName = iconSystemName
        self.title = title
        self.subtitle = subtitle
        self.subtitleIsAccent = subtitleIsAccent
    }
}

/// DApp
public struct DiscoverDApp: Identifiable, Codable {
    public let id: String
    public let logoText: String
    public let logoColorHex: String
    public let title: String
    public let subtitle: String
    public let actionTitle: String

    public init(id: String, logoText: String, logoColorHex: String, title: String, subtitle: String, actionTitle: String) {
        self.id = id
        self.logoText = logoText
        self.logoColorHex = logoColorHex
        self.title = title
        self.subtitle = subtitle
        self.actionTitle = actionTitle
    }
}
```

- [ ] **Step 2: 编译验证**

Run: `./dev/run-simulator.sh`
Expected: 编译成功

---

## Task 3: ViewModel（mock 数据）

**Files:**
- Create: `submodules/TelegramCustom/Sources/Features/Discover/ViewModels/DiscoverViewModel.swift`

- [ ] **Step 1: 创建 ViewModel**

```swift
import Foundation
import SwiftUI
import TelegramCustomCore

public final class DiscoverViewModel: ObservableObject {
    @Published public var banners: [DiscoverBanner] = []
    @Published public var tools: [DiscoverTool] = []
    @Published public var dApps: [DiscoverDApp] = []

    public init() {
        self.loadMockData()
    }

    private func loadMockData() {
        // 3 张相同 Banner（后续换成接口数据）
        let banner = DiscoverBanner(
            id: "banner-1",
            tag: "discover.banner.tag".localized,
            title: "discover.banner.title".localized,
            subtitle: "discover.banner.subtitle".localized,
            actionTitle: "discover.banner.action".localized
        )
        self.banners = [banner, banner, banner]

        self.tools = [
            DiscoverTool(
                id: "meeting",
                iconSystemName: "video.fill",
                title: "discover.tool.meeting".localized,
                subtitle: "discover.tool.meeting.desc".localized
            ),
            DiscoverTool(
                id: "vpn",
                iconSystemName: "shield.lefthalf.filled",
                title: "discover.tool.vpn".localized,
                subtitle: "discover.tool.vpn.desc".localized,
                subtitleIsAccent: true
            ),
            DiscoverTool(
                id: "escrow",
                iconSystemName: "checkmark.shield.fill",
                title: "discover.tool.escrow".localized,
                subtitle: "discover.tool.escrow.desc".localized
            )
        ]

        self.dApps = [
            DiscoverDApp(
                id: "uniswap",
                logoText: "UNI",
                logoColorHex: "EC4899",
                title: "Uniswap V3",
                subtitle: "discover.dapp.uniswap.desc".localized,
                actionTitle: "discover.dapp.action".localized
            ),
            DiscoverDApp(
                id: "opensea",
                logoText: "OS",
                logoColorHex: "3B82F6",
                title: "OpenSea NFT Hub",
                subtitle: "discover.dapp.opensea.desc".localized,
                actionTitle: "discover.dapp.action".localized
            )
        ]
    }
}
```

- [ ] **Step 2: 编译验证**

Run: `./dev/run-simulator.sh`
Expected: 编译成功

---

## Task 4: 组件（Banner 轮播 + 工具宫格 + DApp 列表）

**Files:**
- Create: `submodules/TelegramCustom/Sources/Features/Discover/Views/Components/HeroBannerCarousel.swift`
- Create: `submodules/TelegramCustom/Sources/Features/Discover/Views/Components/ToolGridView.swift`
- Create: `submodules/TelegramCustom/Sources/Features/Discover/Views/Components/DAppListView.swift`

- [ ] **Step 1: HeroBannerCarousel（3 张自动轮播 + 圆点）**

```swift
import SwiftUI
import TelegramCustomCore

struct HeroBannerCarousel: View {
    let banners: [DiscoverBanner]
    @State private var currentIndex = 0
    private let timer = Timer.publish(every: 3.0, on: .main, in: .common).autoconnect()

    var body: some View {
        VStack(spacing: 8) {
            TabView(selection: $currentIndex) {
                ForEach(Array(banners.enumerated()), id: \.offset) { index, banner in
                    bannerCard(banner)
                        .tag(index)
                }
            }
            .tabViewStyle(PageTabViewStyle(indexDisplayMode: .never))
            .frame(height: 144)

            // 圆点指示器
            HStack(spacing: 4) {
                ForEach(0..<banners.count, id: \.self) { index in
                    Circle()
                        .fill(index == currentIndex ? ColorPalette.brandNeon : Color.white.opacity(0.3))
                        .frame(width: 8, height: 8)
                }
            }
        }
        .onReceive(timer) { _ in
            withAnimation {
                currentIndex = (currentIndex + 1) % banners.count
            }
        }
    }

    private func bannerCard(_ banner: DiscoverBanner) -> some View {
        VStack(alignment: .leading, spacing: 4) {
            HStack {
                Text(banner.tag)
                    .font(.system(size: 10, weight: .bold))
                    .foregroundColor(ColorPalette.brandNeon)
                    .padding(.horizontal, 8)
                    .padding(.vertical, 2)
                    .background(ColorPalette.brandNeon.opacity(0.2))
                    .overlay(
                        RoundedRectangle(cornerRadius: 4)
                            .stroke(ColorPalette.brandNeon.opacity(0.4), lineWidth: 1)
                    )
                Spacer()
            }
            Spacer()
            Text(banner.title)
                .font(.system(size: 16, weight: .heavy))
                .foregroundColor(.white)
            Text(banner.subtitle)
                .font(.system(size: 12))
                .foregroundColor(ColorPalette.textSub)
            Spacer()
            HStack {
                Text(banner.actionTitle)
                    .font(.system(size: 12, weight: .bold))
                    .foregroundColor(.black)
                    .padding(.horizontal, 12)
                    .padding(.vertical, 6)
                    .background(ColorPalette.brandNeon)
                    .cornerRadius(12)
                Spacer()
            }
        }
        .padding(16)
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(
            LinearGradient(
                gradient: Gradient(colors: [
                    Color(hex: "022C22"),
                    ColorPalette.surface2,
                    ColorPalette.backgroundBlack
                ]),
                startPoint: .leading,
                endPoint: .trailing
            )
        )
        .cornerRadius(16)
        .overlay(
            RoundedRectangle(cornerRadius: 16)
                .stroke(ColorPalette.brandNeon.opacity(0.3), lineWidth: 1)
        )
        .padding(.horizontal, 16)
    }
}
```

- [ ] **Step 2: ToolGridView（3 宫格）**

```swift
import SwiftUI
import TelegramCustomCore

struct ToolGridView: View {
    let tools: [DiscoverTool]
    let onTap: (DiscoverTool) -> Void

    var body: some View {
        LazyVGrid(columns: Array(repeating: GridItem(.flexible(), spacing: 12), count: 3), spacing: 12) {
            ForEach(tools) { tool in
                Button(action: { onTap(tool) }) {
                    VStack(spacing: 8) {
                        ZStack {
                            RoundedRectangle(cornerRadius: 12)
                                .fill(ColorPalette.surface2)
                                .frame(width: 40, height: 40)
                                .overlay(
                                    RoundedRectangle(cornerRadius: 12)
                                        .stroke(ColorPalette.borderClr, lineWidth: 1)
                                )
                            Image(systemName: tool.iconSystemName)
                                .font(.system(size: 18))
                                .foregroundColor(ColorPalette.brandNeon)
                        }
                        Text(tool.title)
                            .font(.system(size: 12, weight: .bold))
                            .foregroundColor(.white)
                        Text(tool.subtitle)
                            .font(.system(size: 10))
                            .foregroundColor(tool.subtitleIsAccent ? ColorPalette.brandNeon : ColorPalette.txtMuted)
                    }
                    .frame(maxWidth: .infinity)
                    .padding(12)
                    .background(ColorPalette.surface1)
                    .cornerRadius(16)
                    .overlay(
                        RoundedRectangle(cornerRadius: 16)
                            .stroke(ColorPalette.borderClr, lineWidth: 1)
                    )
                }
                .buttonStyle(PlainButtonStyle())
            }
        }
    }
}
```

- [ ] **Step 3: DAppListView**

```swift
import SwiftUI
import TelegramCustomCore

struct DAppListView: View {
    let dApps: [DiscoverDApp]
    let onTap: (DiscoverDApp) -> Void

    var body: some View {
        VStack(spacing: 8) {
            ForEach(dApps) { dApp in
                Button(action: { onTap(dApp) }) {
                    HStack(spacing: 12) {
                        ZStack {
                            RoundedRectangle(cornerRadius: 12)
                                .fill(Color(hex: dApp.logoColorHex).opacity(0.15))
                                .frame(width: 40, height: 40)
                                .overlay(
                                    RoundedRectangle(cornerRadius: 12)
                                        .stroke(Color(hex: dApp.logoColorHex).opacity(0.3), lineWidth: 1)
                                )
                            Text(dApp.logoText)
                                .font(.system(size: 14, weight: .heavy))
                                .foregroundColor(Color(hex: dApp.logoColorHex))
                        }
                        VStack(alignment: .leading, spacing: 2) {
                            Text(dApp.title)
                                .font(.system(size: 12, weight: .bold))
                                .foregroundColor(.white)
                            Text(dApp.subtitle)
                                .font(.system(size: 10))
                                .foregroundColor(ColorPalette.textSub)
                        }
                        Spacer()
                        Text(dApp.actionTitle)
                            .font(.system(size: 10, weight: .bold))
                            .foregroundColor(ColorPalette.textSub)
                            .padding(.horizontal, 10)
                            .padding(.vertical, 4)
                            .background(ColorPalette.surface2)
                            .cornerRadius(8)
                            .overlay(
                                RoundedRectangle(cornerRadius: 8)
                                    .stroke(ColorPalette.borderClr, lineWidth: 1)
                            )
                    }
                    .padding(12)
                    .background(ColorPalette.surface1)
                    .cornerRadius(16)
                    .overlay(
                        RoundedRectangle(cornerRadius: 16)
                            .stroke(ColorPalette.borderClr, lineWidth: 1)
                    )
                }
                .buttonStyle(PlainButtonStyle())
            }
        }
    }
}
```

- [ ] **Step 4: 编译验证**

Run: `./dev/run-simulator.sh`
Expected: 编译成功

---

## Task 5: 主视图（Segmented Control + 内容）

**Files:**
- Create: `submodules/TelegramCustom/Sources/Features/Discover/Views/DiscoverScreenView.swift`
- Create: `submodules/TelegramCustom/Sources/Features/Discover/Views/DiscoverExploreView.swift`
- Create: `submodules/TelegramCustom/Sources/Features/Discover/Views/DiscoverWalletPlaceholderView.swift`

- [ ] **Step 1: DiscoverScreenView（主容器）**

```swift
import SwiftUI
import TelegramCustomCore

public enum DiscoverTab: Int {
    case wallet = 0
    case explore = 1
}

public struct DiscoverScreenView: View {
    @StateObject private var viewModel = DiscoverViewModel()
    @State private var selectedTab: DiscoverTab = .explore
    @State private var showToast = false

    public init() {}

    public var body: some View {
        VStack(spacing: 0) {
            segmentedControl
            if selectedTab == .explore {
                DiscoverExploreView(
                    viewModel: viewModel,
                    onTap: { showToast = true }
                )
            } else {
                DiscoverWalletPlaceholderView()
            }
        }
        .background(ColorPalette.backgroundBlack)
        .overlay(toastOverlay)
    }

    private var segmentedControl: some View {
        HStack {
            HStack(spacing: 0) {
                segmentButton(tab: .wallet, icon: "wallet", title: "discover.tab.wallet".localized)
                segmentButton(tab: .explore, icon: "compass", title: "discover.tab.explore".localized)
            }
            .padding(4)
            .background(ColorPalette.surface2)
            .cornerRadius(12)
            .overlay(
                RoundedRectangle(cornerRadius: 12)
                    .stroke(ColorPalette.borderClr, lineWidth: 1)
            )
        }
        .frame(maxWidth: .infinity)
        .padding(.horizontal, 16)
        .padding(.vertical, 12)
        .background(ColorPalette.backgroundBlack)
        .overlay(
            Rectangle()
                .frame(height: 1)
                .foregroundColor(ColorPalette.borderClr),
            alignment: .bottom
        )
    }

    private func segmentButton(tab: DiscoverTab, icon: String, title: String) -> some View {
        let isSelected = selectedTab == tab
        return Button(action: { selectedTab = tab }) {
            HStack(spacing: 4) {
                Image(systemName: icon)
                    .font(.system(size: 12))
                Text(title)
                    .font(.system(size: 12, weight: .bold))
            }
            .foregroundColor(isSelected ? .black : ColorPalette.textSub)
            .padding(.vertical, 6)
            .frame(maxWidth: .infinity)
            .background(isSelected ? ColorPalette.brandNeon : Color.clear)
            .cornerRadius(8)
        }
        .buttonStyle(PlainButtonStyle())
    }

    private var toastOverlay: some View {
        Group {
            if showToast {
                Text("discover.comingSoon".localized)
                    .font(.system(size: 12))
                    .foregroundColor(.white)
                    .padding(.horizontal, 16)
                    .padding(.vertical, 8)
                    .background(ColorPalette.surface2)
                    .cornerRadius(12)
                    .overlay(
                        RoundedRectangle(cornerRadius: 12)
                            .stroke(ColorPalette.brandNeon.opacity(0.4), lineWidth: 1)
                    )
                    .onAppear {
                        DispatchQueue.main.asyncAfter(deadline: .now() + 2.5) {
                            showToast = false
                        }
                    }
            }
        }
    }
}
```

- [ ] **Step 2: DiscoverExploreView**

```swift
import SwiftUI
import TelegramCustomCore

struct DiscoverExploreView: View {
    @ObservedObject var viewModel: DiscoverViewModel
    let onTap: () -> Void

    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 20) {
                HeroBannerCarousel(banners: viewModel.banners)

                VStack(alignment: .leading, spacing: 10) {
                    sectionTitle("discover.tools.section".localized)
                    ToolGridView(tools: viewModel.tools) { _ in onTap() }
                }
                .padding(.horizontal, 16)

                VStack(alignment: .leading, spacing: 10) {
                    sectionTitle("discover.dapp.section".localized)
                    DAppListView(dApps: viewModel.dApps) { _ in onTap() }
                }
                .padding(.horizontal, 16)
            }
            .padding(.vertical, 16)
        }
        .background(ColorPalette.backgroundBlack)
    }

    private func sectionTitle(_ text: String) -> some View {
        Text(text.uppercased())
            .font(.system(size: 12, weight: .bold))
            .foregroundColor(ColorPalette.textSub)
            .kerning(1.0)
    }
}
```

- [ ] **Step 3: DiscoverWalletPlaceholderView（钱包留白）**

```swift
import SwiftUI
import TelegramCustomCore

struct DiscoverWalletPlaceholderView: View {
    var body: some View {
        VStack(spacing: 20) {
            Image(systemName: "wallet")
                .font(.system(size: 64))
                .foregroundColor(ColorPalette.brandNeon)
            Text("discover.comingSoon".localized)
                .font(.system(size: 24, weight: .bold))
                .foregroundColor(.white)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(ColorPalette.backgroundBlack)
    }
}
```

- [ ] **Step 4: 编译验证**

Run: `./dev/run-simulator.sh`
Expected: 编译成功

---

## Task 6: DiscoverViewController（UIKit 容器）

**Files:**
- Create: `submodules/TelegramCustom/Sources/Features/Discover/DiscoverViewController.swift`

- [ ] **Step 1: 创建 UIKit 容器**

```swift
import Foundation
import UIKit
import SwiftUI
import Display
import AsyncDisplayKit

public final class DiscoverViewController: ViewController {
    private var hostingController: UIHostingController<DiscoverScreenView>?

    public override init(navigationBarPresentationData: NavigationBarPresentationData?) {
        super.init(navigationBarPresentationData: navigationBarPresentationData)
        self.statusBar.statusBarStyle = .White
    }

    required public init(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    public override func loadDisplayNode() {
        self.displayNode = ASDisplayNode()
        self.displayNode.backgroundColor = .black
        self.displayNodeDidLoad()
    }

    public override func displayNodeDidLoad() {
        super.displayNodeDidLoad()

        let hostingController = UIHostingController(rootView: DiscoverScreenView())
        hostingController.view.backgroundColor = .black
        self.hostingController = hostingController

        self.addChild(hostingController)
        self.displayNode.view.addSubview(hostingController.view)
        hostingController.didMove(toParent: self)
    }

    public override func containerLayoutUpdated(_ layout: ContainerViewLayout, transition: ContainedViewLayoutTransition) {
        super.containerLayoutUpdated(layout, transition: transition)
        self.hostingController?.view.frame = CGRect(origin: .zero, size: layout.size)
    }
}
```

- [ ] **Step 2: 编译验证**

Run: `./dev/run-simulator.sh`
Expected: 编译成功

---

## Task 7: 多语言字符串

**Files:**
- Modify: `submodules/TelegramCustom/Resources/Localizations/zh-Hans.lproj/Localizable.strings`
- Modify: `submodules/TelegramCustom/Resources/Localizations/en.lproj/Localizable.strings`
- Modify: `submodules/TelegramCustom/Resources/Localizations/ja.lproj/Localizable.strings`
- Modify: `submodules/TelegramCustom/Resources/Localizations/ko.lproj/Localizable.strings`

- [ ] **Step 1: 中文（zh-Hans）**

在文件末尾添加：
```
"discover.tab.wallet" = "Web3 钱包";
"discover.tab.explore" = "探索生态";
"discover.banner.tag" = "超级生态推广";
"discover.banner.title" = "Sui & ETH 跨链 Gasless 交易节";
"discover.banner.subtitle" = "无感知签名，平台补贴 100% 链上 Gas 费";
"discover.banner.action" = "立即参与 →";
"discover.tools.section" = "核心工具能力";
"discover.tool.meeting" = "Web3 会议";
"discover.tool.meeting.desc" = "高清/可控录制";
"discover.tool.vpn" = "安全 VPN";
"discover.tool.vpn.desc" = "剩余 8.5 GB";
"discover.tool.escrow" = "u多担保";
"discover.tool.escrow.desc" = "多签/防骗查验";
"discover.dapp.section" = "热门 Web3 DApp";
"discover.dapp.uniswap.desc" = "去中心化 Swap 交易所";
"discover.dapp.opensea.desc" = "NFT 交易与收藏品市场";
"discover.dapp.action" = "调用";
"discover.comingSoon" = "即将推出";
```

- [ ] **Step 2: 英文（en）**

```
"discover.tab.wallet" = "Web3 Wallet";
"discover.tab.explore" = "Explore";
"discover.banner.tag" = "Super Ecosystem Promo";
"discover.banner.title" = "Sui & ETH Cross-chain Gasless Trading Festival";
"discover.banner.subtitle" = "Seamless signing, platform covers 100% on-chain gas fees";
"discover.banner.action" = "Join Now →";
"discover.tools.section" = "Core Tools";
"discover.tool.meeting" = "Web3 Meeting";
"discover.tool.meeting.desc" = "HD/Controllable Recording";
"discover.tool.vpn" = "Secure VPN";
"discover.tool.vpn.desc" = "8.5 GB Remaining";
"discover.tool.escrow" = "u-Duo Escrow";
"discover.tool.escrow.desc" = "Multi-sig/Anti-fraud";
"discover.dapp.section" = "Hot Web3 DApps";
"discover.dapp.uniswap.desc" = "Decentralized Swap Exchange";
"discover.dapp.opensea.desc" = "NFT Marketplace";
"discover.dapp.action" = "Open";
"discover.comingSoon" = "Coming Soon";
```

- [ ] **Step 3: 日文（ja）**

```
"discover.tab.wallet" = "Web3 ウォレット";
"discover.tab.explore" = "エコシステム探索";
"discover.banner.tag" = "スーパーエコシステムプロモ";
"discover.banner.title" = "Sui & ETH クロスチェーン Gasless 取引フェス";
"discover.banner.subtitle" = "シームレス署名、プラットフォームがオンチェーンガス代100%負担";
"discover.banner.action" = "今すぐ参加 →";
"discover.tools.section" = "コアツール";
"discover.tool.meeting" = "Web3 会議";
"discover.tool.meeting.desc" = "高画質/制御可能な録画";
"discover.tool.vpn" = "セキュア VPN";
"discover.tool.vpn.desc" = "残り 8.5 GB";
"discover.tool.escrow" = "u多保証";
"discover.tool.escrow.desc" = "マルチシグ/詐欺防止検証";
"discover.dapp.section" = "人気 Web3 DApp";
"discover.dapp.uniswap.desc" = "分散型 Swap 取引所";
"discover.dapp.opensea.desc" = "NFT マーケットプレイス";
"discover.dapp.action" = "開く";
"discover.comingSoon" = "近日公開";
```

- [ ] **Step 4: 韩文（ko）**

```
"discover.tab.wallet" = "Web3 지갑";
"discover.tab.explore" = "생태계 탐색";
"discover.banner.tag" = "슈퍼 생태계 프로모션";
"discover.banner.title" = "Sui & ETH 크로스체인 가스리스 트레이딩 페스티벌";
"discover.banner.subtitle" = "원활한 서명, 플랫폼이 온체인 가스비 100% 지원";
"discover.banner.action" = "지금 참여 →";
"discover.tools.section" = "핵심 도구";
"discover.tool.meeting" = "Web3 회의";
"discover.tool.meeting.desc" = "고화질/제어 가능한 녹화";
"discover.tool.vpn" = "보안 VPN";
"discover.tool.vpn.desc" = "8.5 GB 남음";
"discover.tool.escrow" = "u다오 보증";
"discover.tool.escrow.desc" = "멀티시그/사기 방지 검증";
"discover.dapp.section" = "인기 Web3 DApp";
"discover.dapp.uniswap.desc" = "탈중앙화 스왑 거래소";
"discover.dapp.opensea.desc" = "NFT 마켓플레이스";
"discover.dapp.action" = "열기";
"discover.comingSoon" = "곧 출시";
```

- [ ] **Step 5: 编译验证**

Run: `./dev/run-simulator.sh`
Expected: 编译成功

---

## Task 8: BUILD 配置 + 导出

**Files:**
- Modify: `submodules/TelegramCustom/BUILD`
- Modify: `submodules/TelegramCustom/Sources/TelegramCustom.swift`

- [ ] **Step 1: BUILD 添加 Discover 模块**

在 `submodules/TelegramCustom/BUILD` 添加：

```python
swift_library(
    name = "DiscoverModule",
    module_name = "Discover",
    srcs = glob([
        "Sources/Features/Discover/**/*.swift",
    ]),
    deps = [
        ":TelegramCustomCore",
        "//submodules/Display:Display",
        "//submodules/AsyncDisplayKit:AsyncDisplayKit",
    ],
    visibility = ["//visibility:public"],
)
```

然后在 `TelegramCustom` 的 `deps` 数组添加 `":DiscoverModule",`。

- [ ] **Step 2: TelegramCustom.swift 导出**

在 `submodules/TelegramCustom/Sources/TelegramCustom.swift` 添加：

```swift
@_exported import Discover
```

- [ ] **Step 3: 编译验证**

Run: `./dev/run-simulator.sh`
Expected: 编译成功

---

## Task 9: 集成（替换占位页）

**Files:**
- Modify: `submodules/TelegramUI/Sources/TelegramRootController.swift`

- [ ] **Step 1: 替换 DiscoverPlaceholderViewController 为 DiscoverViewController**

在 `addRootControllers` 的 `#if ENABLE_WEB3_SPLASH` 块里，把：

```swift
let discoverController = DiscoverPlaceholderViewController(navigationBarPresentationData: nil)
```

改为：

```swift
let discoverController = DiscoverViewController(navigationBarPresentationData: nil)
```

`updateRootControllers` 里同样替换。

- [ ] **Step 2: 编译验证**

Run: `./dev/run-simulator.sh`
Expected: 编译成功

---

## Task 10: 测试

- [ ] **Step 1: 部署到模拟器（cp 覆盖，避开 simctl install 缓存）**

```bash
K3=7167319A-CA67-41CA-A2D6-C6477A5EAD5B
BUNDLE=live.bchat.origin
SRC="$(find -L bazel-out -maxdepth 14 -path '*/Telegram_archive-root/Payload/Telegram.app' -type d | head -1)"
DEST="$(xcrun simctl get_app_container "$K3" "$BUNDLE" app)"
xcrun simctl terminate "$K3" "$BUNDLE" 2>/dev/null
rm -rf "$DEST" && cp -Rp "$SRC" "$DEST"
xcrun simctl launch "$K3" "$BUNDLE"
```

- [ ] **Step 2: 手动测试清单**

- [ ] 发现页 Tab 显示 Segmented Control（钱包 | 探索生态），默认选中探索生态
- [ ] Banner 轮播 3 张自动切换 + 圆点指示器
- [ ] 核心工具 3 宫格（图标 + 名称 + 描述），VPN 描述荧光绿
- [ ] 热门 DApp 列表（UNI 粉 / OS 蓝 logo + 调用按钮）
- [ ] 点击卡片弹「即将推出」轻提示（2.5 秒消失）
- [ ] 切到「钱包」显示留白
- [ ] 多语言切换正常
- [ ] 视觉 1:1（颜色/字号/间距/圆角）

---

## 完成检查清单

- [ ] 所有代码在 TelegramCustom 模块（无官方源码修改，除 TelegramRootController 集成）
- [ ] TelegramRootController 集成用 `#if ENABLE_WEB3_SPLASH` 包裹
- [ ] 无调试 print
- [ ] 多语言 4 语言齐全
- [ ] 数据模型 Codable，可接口替换

---

**预计实现时间**: 半天
**优先级**: P1
